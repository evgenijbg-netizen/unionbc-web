---
phase: 02-content-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/product/HeroSection.astro
  - src/components/product/SpecsTable.astro
  - src/components/product/HowItWorks.astro
  - src/components/product/AdvantagesList.astro
  - src/components/product/ComparisonTable.astro
  - src/components/shared/SectionHeading.astro
  - src/pages/cs/produkt.astro
  - src/pages/en/product.astro
  - src/pages/cs/index.astro
  - src/pages/en/index.astro
  - src/i18n/cs.json
  - src/i18n/en.json
autonomous: true

must_haves:
  truths:
    - "Visitor lands on the homepage and within 10 seconds sees what TC101 is, its key stats, and a contact CTA"
    - "Visitor can navigate to a product page showing technical specs in a data table, how-it-works explanation, advantages list, and side-by-side comparison with conventional insulation"
  artifacts:
    - path: "src/pages/cs/produkt.astro"
      provides: "Czech product page composing all section components"
      contains: "BaseLayout"
    - path: "src/pages/en/product.astro"
      provides: "English product page mirroring Czech structure"
      contains: "BaseLayout"
    - path: "src/components/product/SpecsTable.astro"
      provides: "Technical specifications data table"
      contains: "table"
    - path: "src/components/product/HowItWorks.astro"
      provides: "How ceramic microspheres work explanation"
      min_lines: 20
    - path: "src/components/product/AdvantagesList.astro"
      provides: "Structured list of product advantages"
      min_lines: 15
    - path: "src/components/product/ComparisonTable.astro"
      provides: "Side-by-side TC101 vs conventional insulation"
      contains: "table"
    - path: "src/components/product/HeroSection.astro"
      provides: "Product page hero with value proposition and CTA"
      min_lines: 15
    - path: "src/components/shared/SectionHeading.astro"
      provides: "Reusable section heading component"
    - path: "src/pages/cs/index.astro"
      provides: "Enhanced Czech homepage with full content"
      min_lines: 40
    - path: "src/pages/en/index.astro"
      provides: "Enhanced English homepage mirroring Czech"
      min_lines: 40
  key_links:
    - from: "src/pages/cs/produkt.astro"
      to: "src/components/product/*.astro"
      via: "import and compose section components"
      pattern: "import.*from.*components/product"
    - from: "src/components/product/SpecsTable.astro"
      to: "src/i18n/utils.ts"
      via: "t(locale, key) for all spec labels and values"
      pattern: "t\\(locale"
    - from: "src/pages/cs/index.astro"
      to: "src/i18n/routes.ts"
      via: "getLocalizedPath for CTA links"
      pattern: "getLocalizedPath"
---

<objective>
Create the product page with all five content sections (hero, specs table, how-it-works, advantages, comparison) and enhance the homepage from a placeholder into a full content page with feature highlights, expanded stats, and clear value proposition.

Purpose: Delivers the core product presentation (PROD-01 through PROD-05) and homepage (part of PROD-01) -- the two most important pages for visitor understanding of TC101.
Output: Product page with 5 section components, enhanced homepage, ~120 new translation keys per language.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-conversion/02-RESEARCH.md
@src/i18n/utils.ts
@src/i18n/routes.ts
@src/i18n/cs.json
@src/i18n/en.json
@src/layouts/BaseLayout.astro
@src/components/Header.astro
@src/components/Footer.astro
@src/pages/cs/index.astro
@src/pages/en/index.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create product page with five section components</name>
  <files>
    src/components/product/HeroSection.astro
    src/components/product/SpecsTable.astro
    src/components/product/HowItWorks.astro
    src/components/product/AdvantagesList.astro
    src/components/product/ComparisonTable.astro
    src/components/shared/SectionHeading.astro
    src/pages/cs/produkt.astro
    src/pages/en/product.astro
    src/i18n/cs.json
    src/i18n/en.json
  </files>
  <action>
    Create the product page as a composition of five section components. Each component accepts `locale: Locale` as prop and uses `t(locale, key)` for ALL text. Zero hardcoded strings.

    **CRITICAL content rules:**
    - All text written from scratch in native modern Czech and native English. DO NOT translate from one language to the other. Write each language independently with natural phrasing.
    - Technical claims must include measurement context (e.g., "50x lower thermal conductivity than mineral wool" not just "0.001 W/m.C")
    - Reference Czech certifications (Certifikat vyrobku, STO) when backing technical claims per QUAL-02
    - Page file names MUST match route map slugs exactly: cs=`produkt`, en=`product`

    **SectionHeading.astro** (shared component):
    - Accepts `title: string` and optional `subtitle: string` props
    - Renders h2 with `text-3xl md:text-4xl font-heading font-bold text-text-primary mb-4`
    - Optional subtitle in `text-text-secondary`
    - Used by all section components for consistent heading style

    **HeroSection.astro:**
    - Full-width hero with product name, 1-liner value proposition, key stat (e.g., "1mm replaces 50mm"), and CTA button
    - Background: subtle gradient or surface-800 to distinguish from page body
    - CTA links to contact page via `getLocalizedPath("contact", locale)`
    - Translation keys: `product.hero.title`, `product.hero.subtitle`, `product.hero.description`, `product.hero.stat`, `product.hero.statLabel`

    **SpecsTable.astro:**
    - HTML `<table>` with `<thead>` and `<tbody>`, styled with Tailwind
    - Two columns: Parameter (left-aligned) and Value (right-aligned, accent color)
    - Specs from research content inventory: density (410 kg/m3), thermal conductivity (0.001 W/m.C), heat capacity (1.10 kJ/kg.C), temp range (-60 to +260 C), vapor permeability (0.02 mg/m.h.Pa), weight (0.65 kg/L), elongation (up to 80%), consumption (~0.34 kg/m2 at 0.5mm)
    - Each spec has a `.context` key providing human-readable comparison (e.g., "Lighter than water -- does not stress insulated structures")
    - Context row shown below value in smaller text-text-secondary
    - Table wrapped in `overflow-x-auto` for mobile horizontal scroll
    - Translation keys: `product.specs.title`, `product.specs.parameterLabel`, `product.specs.valueLabel`, then `product.specs.{specKey}.label`, `.value`, `.context` for each spec

    **HowItWorks.astro:**
    - Explains ceramic microsphere technology: heat reflection vs R-value insulation
    - 3 numbered steps or concept blocks (NOT a wall of text): 1) Ceramic microspheres in latex binder, 2) Reflects radiant heat instead of resisting conduction, 3) Thin-film application replaces thick conventional insulation
    - Each block: icon/number + title + 2-3 sentence explanation
    - Use numbered card layout with bg-surface-800 cards
    - Translation keys: `product.howItWorks.title`, `product.howItWorks.intro`, `product.howItWorks.step{N}.title`, `product.howItWorks.step{N}.description`

    **AdvantagesList.astro:**
    - Grid of advantage cards (2 cols on md, 1 on mobile)
    - Each card: icon placeholder (SVG checkmark or similar) + title + short description
    - Advantages: time savings (paint-on application), no scaffolding needed, fire-resistant, lightweight (doesn't stress structures), anti-corrosion under insulation (CUI prevention), vapor permeable, flexible (up to 80% elongation), wide temp range
    - Translation keys: `product.advantages.title`, `product.advantages.{advKey}.title`, `product.advantages.{advKey}.description`

    **ComparisonTable.astro:**
    - 3-column table: Parameter | TC101 | Conventional Insulation
    - TC101 column values in accent-400 (highlight), conventional in text-secondary
    - Comparison rows: insulation thickness, weight per m2, installation time, CUI risk, maintenance needs, scaffolding required, fire rating, flexibility on curved surfaces
    - Wrapped in `overflow-x-auto` for mobile
    - Translation keys: `product.comparison.title`, `product.comparison.parameterLabel`, `product.comparison.tc101Label`, `product.comparison.conventionalLabel`, `product.comparison.{rowKey}.label`, `.tc101`, `.conventional`

    **Page files** (cs/produkt.astro and en/product.astro):
    - Thin shells that import BaseLayout and all 5 section components
    - Derive locale from URL, pass to each component
    - Set pageKey="product" on BaseLayout for nav highlighting
    - Meta title and description from product.meta.* keys
    - BOTH page files have IDENTICAL markup structure (only locale from URL differs)

    **Translation keys:**
    - Add ALL product.* keys to both cs.json and en.json
    - Czech content: native modern Czech, technical but accessible. Think "how a Czech engineer would explain this to a procurement manager"
    - English content: native professional English, NOT a translation of the Czech. Independent phrasing that sounds natural to a native English speaker
    - Both languages convey the same facts but with natural phrasing for each language
    - Shared numeric values (specs) can be identical between languages; labels and descriptions MUST be independently written
  </action>
  <verify>
    1. `pnpm build` succeeds with zero errors
    2. `pnpm check` (astro check) passes with zero errors
    3. `pnpm lint` (biome check) passes with zero errors
    4. Built output contains `/cs/produkt/index.html` and `/en/product/index.html`
    5. Both product pages contain `<table>` elements (specs and comparison)
    6. All `product.*` keys exist in both cs.json and en.json with no key returning its raw key string
  </verify>
  <done>
    Product page renders at /cs/produkt/ and /en/product/ with: hero section with CTA, specs data table with 8+ parameters and measurement context, how-it-works with 3 concept blocks, advantages grid with 8 cards, comparison table with 8 rows comparing TC101 vs conventional. All text from translation keys, zero hardcoded strings. Czech and English read as native text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance homepage from placeholder to full content</name>
  <files>
    src/pages/cs/index.astro
    src/pages/en/index.astro
    src/i18n/cs.json
    src/i18n/en.json
  </files>
  <action>
    Expand the current homepage from a basic hero + 3 stat cards into a full landing page that communicates TC101's value within 10 seconds of landing (PROD-01).

    **Current state:** Hero section (title + subtitle + description + CTA) + 3 stat cards (temp range, conductivity, application).

    **Enhance to include these sections (in order):**

    1. **Hero section (enhance existing):**
       - Keep the TEMP-COAT TC101 heading and subtitle
       - Add a stronger value proposition line: "1mm replaces 50mm of conventional insulation"
       - Keep the CTA button, add a secondary "Learn more" link to product page
       - Add a max-w-4xl container for better readability

    2. **Key stats section (enhance existing):**
       - Expand from 3 to 4 stat cards: add "Certifikovano v CR" / "Certified in CZ" card
       - Keep the existing grid layout (lg:grid-cols-4 now)

    3. **Why TC101 section (NEW):**
       - Brief 3-column feature highlight: "Saves time", "Saves money", "Proven technology"
       - Each: icon placeholder + title + 1-2 sentence description
       - Background: bg-surface-800 full-width section for visual separation
       - Translation keys: `home.why.title`, `home.why.{featureKey}.title`, `home.why.{featureKey}.description`

    4. **Industries section (NEW):**
       - "Trusted across industries" heading
       - Simple tag-cloud or pill list of industries: cement, refinery, food, brewery, paper, construction, marine
       - Lightweight -- just establishes breadth of use, not detailed examples (those go on resources page in 02-02)
       - Translation keys: `home.industries.title`, `home.industries.subtitle`, `home.industries.{industryKey}`

    5. **CTA section (NEW):**
       - Full-width call-to-action band before footer
       - Heading: "Ready to reduce energy costs?" / "Pripraveni snizit naklady na energii?"
       - CTA button linking to contact page
       - Translation keys: `home.cta.title`, `home.cta.description`, `home.cta.button`

    **Content rules:**
    - All text via t(locale, key) -- zero hardcoded strings
    - Czech and English written independently, native phrasing
    - The homepage should make a visitor understand what TC101 is within 10 seconds (hero + stats), why it matters within 30 seconds (why section), and who uses it within 60 seconds (industries)
    - Keep the industrial dark design system: bg-surface-900 body, bg-surface-800 accent sections, accent-400/500 highlights

    **Translation JSON update:**
    - Add all new home.* keys to both cs.json and en.json
    - Keep existing home.* keys, modify only if improving content quality
    - Remove `home.coming_soon` key from both files (no longer needed)
  </action>
  <verify>
    1. `pnpm build` succeeds with zero errors
    2. `pnpm check` passes with zero errors
    3. `pnpm lint` passes with zero errors
    4. Homepage at /cs/ has 5 distinct sections visible (hero, stats, why, industries, CTA)
    5. Homepage at /en/ mirrors the same 5 sections
    6. `home.coming_soon` key removed from both JSON files
    7. All new `home.*` keys exist in both cs.json and en.json
  </verify>
  <done>
    Homepage at /cs/ and /en/ displays: enhanced hero with value prop and dual CTAs, 4 stat cards including certification, "Why TC101" 3-column feature section, industries pill list, full-width CTA band. Visitor can understand what TC101 is, see key stats, and reach contact within 10 seconds. Content is native Czech and English.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` produces /cs/index.html, /en/index.html, /cs/produkt/index.html, /en/product/index.html with zero errors
- `pnpm check` and `pnpm lint` both pass clean
- Product page contains all 5 sections rendered from dedicated components
- Homepage contains 5 content sections (hero, stats, why, industries, CTA)
- Navigation "Produkt TC101" / "Product TC101" link resolves to product page (no 404)
- Language switcher on product page correctly maps between /cs/produkt/ and /en/product/
- All visible text on both pages comes from translation JSON files
</verification>

<success_criteria>
- PROD-01: Homepage shows value proposition, key stats, and contact CTA within 10 seconds -- COVERED by enhanced homepage hero + stats + CTA
- PROD-02: Technical specs in structured data table -- COVERED by SpecsTable component with 8+ parameters
- PROD-03: How it works explanation -- COVERED by HowItWorks component with 3 concept blocks on ceramic microsphere technology
- PROD-04: Structured advantages list -- COVERED by AdvantagesList component with 8 advantage cards
- PROD-05: Side-by-side comparison -- COVERED by ComparisonTable component with 8 comparison rows
- QUAL-01: Content from scratch in modern Czech -- COVERED by independent Czech content writing
- QUAL-02: Claims framed around Czech certifications -- COVERED by measurement context in specs and certification references
- QUAL-03: Native English parity -- COVERED by independently-written English content
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-conversion/02-01-SUMMARY.md`
</output>
