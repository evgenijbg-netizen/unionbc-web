---
phase: 02-content-conversion
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/pages/cs/kontakt.astro
  - src/pages/en/contact.astro
  - src/components/shared/StickyCtaBar.astro
  - src/layouts/BaseLayout.astro
  - src/i18n/cs.json
  - src/i18n/en.json
autonomous: false

must_haves:
  truths:
    - "Contact information (phone, email, address) is visible in the footer on every page and on a dedicated contact page"
    - "A persistent sticky Request consultation CTA is visible during scroll on mobile"
    - "All content reads as native modern Czech and native English written from scratch"
    - "CZ and EN translation files have exactly the same set of keys (full parity)"
  artifacts:
    - path: "src/pages/cs/kontakt.astro"
      provides: "Czech contact page with full company details"
      contains: "BaseLayout"
    - path: "src/pages/en/contact.astro"
      provides: "English contact page"
      contains: "BaseLayout"
    - path: "src/components/shared/StickyCtaBar.astro"
      provides: "Persistent mobile CTA bar"
      contains: "fixed"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Updated layout with StickyCtaBar and bottom padding"
      contains: "StickyCtaBar"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/shared/StickyCtaBar.astro"
      via: "import and render after Footer"
      pattern: "import StickyCtaBar"
    - from: "src/components/shared/StickyCtaBar.astro"
      to: "src/i18n/routes.ts"
      via: "getLocalizedPath for contact link"
      pattern: "getLocalizedPath.*contact"
    - from: "src/pages/cs/kontakt.astro"
      to: "src/i18n/utils.ts"
      via: "t(locale, key) for all contact details"
      pattern: "t\\(locale.*contact\\."
---

<objective>
Create the contact page, add a persistent sticky CTA bar to all pages, verify i18n key parity, and get human sign-off on all Phase 2 content.

Purpose: Completes the conversion funnel (CONV-01, CONV-03) and ensures content quality across all pages (QUAL-01, QUAL-02, QUAL-03). The human checkpoint validates that all content reads naturally before moving to Phase 3.
Output: Contact page, sticky CTA on all pages, verified i18n parity, human-approved content.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-conversion/02-RESEARCH.md
@.planning/phases/02-content-conversion/02-01-SUMMARY.md
@.planning/phases/02-content-conversion/02-02-SUMMARY.md
@src/i18n/utils.ts
@src/i18n/routes.ts
@src/i18n/cs.json
@src/i18n/en.json
@src/layouts/BaseLayout.astro
@src/components/Footer.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contact page and sticky CTA bar</name>
  <files>
    src/pages/cs/kontakt.astro
    src/pages/en/contact.astro
    src/components/shared/StickyCtaBar.astro
    src/layouts/BaseLayout.astro
    src/i18n/cs.json
    src/i18n/en.json
  </files>
  <action>
    Create a dedicated contact page and add a persistent sticky CTA bar to the BaseLayout so it appears on all pages.

    **Contact page (cs/kontakt.astro and en/contact.astro):**
    - Page file names MUST match route slugs: cs=`kontakt`, en=`contact`
    - Uses BaseLayout with pageKey="contact"
    - Content sections:
      1. **Page heading:** "Kontaktujte nas" / "Contact Us" with brief subtitle about requesting consultation
      2. **Contact person card:** Halina Bogdanovich, Jednatelka / Managing Director, Distributor TEMP-COAT pro CR / TEMP-COAT distributor for Czech Republic
      3. **Contact details grid (2 cols on md):**
         - Phone: +420 777 832 348 (clickable tel: link)
         - Email: info@unionbc.cz (clickable mailto: link)
         - Address: Jiskrova 1566, Brandys nad Labem, 250 01
         - Company: Union B+C s.r.o., IC (company ID if available)
      4. **Map or address highlight:** A styled card with the full address. No embedded map (avoids external scripts). Just a clean address card with a link to Google Maps.
      5. **CTA section:** "Let's discuss your insulation needs" / "Probereme vase potreby izolace" with phone and email prominently displayed
    - Styling: Clean, spacious layout. Contact cards use bg-surface-800 with accent borders. Phone/email are large and prominent.
    - Translation keys: `contact.meta.title`, `contact.meta.description`, `contact.page.title`, `contact.page.subtitle`, `contact.person.name`, `contact.person.role`, `contact.phone.label`, `contact.email.label`, `contact.address.label`, `contact.address.value`, `contact.company.name`, `contact.company.id`, `contact.cta.title`, `contact.cta.description`, `contact.map.link`

    **StickyCtaBar.astro:**
    - Fixed-position bar at bottom of screen, visible on mobile only (`md:hidden`)
    - Background: bg-surface-800/95 with backdrop-blur-sm and top border
    - Contains single CTA button: "Nezavazna konzultace" / "Free consultation" linking to contact page
    - Uses `getLocalizedPath("contact", locale)` for the link
    - z-40 to stay above content but below header (z-50)
    - Translation: uses existing `cta.consultation` key (already in both JSONs from Phase 1)

    **BaseLayout.astro modifications:**
    - Import StickyCtaBar component
    - Add `<StickyCtaBar locale={locale} />` after `<Footer>` (inside `<body>`, after footer)
    - Add `pb-16 md:pb-0` to `<main>` element to prevent sticky CTA from overlapping last content on mobile (research pitfall #5)
    - Pass locale to StickyCtaBar

    **Content rules:**
    - Contact details: Use translation keys even for phone/email (already exist as `footer.phone`, `footer.email` from Phase 1). The contact page can reference the same keys or create contact-specific ones for richer context.
    - Czech/English: native phrasing, professional tone suitable for B2B industrial context
    - Address in both languages is the same (physical address does not change)
  </action>
  <verify>
    1. `pnpm build` succeeds with zero errors
    2. Built output contains `/cs/kontakt/index.html` and `/en/contact/index.html`
    3. Contact page contains clickable phone (`tel:`) and email (`mailto:`) links
    4. Built HTML for ANY page (e.g., /cs/index.html) contains the sticky CTA bar markup with `fixed` positioning class
    5. Main element in BaseLayout has `pb-16` class for mobile padding
    6. `pnpm check` and `pnpm lint` pass clean
    7. Navigation "Kontakt" / "Contact" link resolves correctly (no 404)
    8. Language switcher maps /cs/kontakt/ <-> /en/contact/ correctly
  </verify>
  <done>
    Contact page at /cs/kontakt/ and /en/contact/ displays contact person info, phone, email, address, and CTA. Sticky CTA bar appears on all pages on mobile, fixed to bottom with consultation link. Main content has bottom padding to prevent CTA overlap. All text from translation keys.
  </done>
</task>

<task type="auto">
  <name>Task 2: i18n parity check and build verification</name>
  <files>
    src/i18n/cs.json
    src/i18n/en.json
  </files>
  <action>
    Run a comprehensive verification of translation key parity between Czech and English, fix any gaps, and verify the full site builds correctly with all Phase 2 content.

    **Step 1: i18n key parity check**
    Run a Node.js one-liner to compare keys in cs.json and en.json:
    ```
    node -e "const cs=Object.keys(require('./src/i18n/cs.json')).sort();const en=Object.keys(require('./src/i18n/en.json')).sort();const mEn=cs.filter(k=>!en.includes(k));const mCs=en.filter(k=>!cs.includes(k));if(mEn.length)console.log('Missing in en.json:',mEn);if(mCs.length)console.log('Missing in cs.json:',mCs);if(!mEn.length&&!mCs.length)console.log('All keys match!');else process.exit(1)"
    ```
    If any keys are missing, add them with proper translations.

    **Step 2: Check for raw key fallbacks**
    After building, grep the built HTML for any translation key patterns that look like they were not translated (t() function returns the raw key as fallback). Search for patterns like `product.specs.` or `faq.q` appearing as visible text in the HTML output. These indicate missing translation keys.
    ```
    grep -r "product\.\|faq\.\|certificates\.\|contact\.\|guide\.\|home\." dist/ --include="*.html" | grep -v "href\|class\|id\|meta\|script"
    ```
    Fix any raw key fallbacks found.

    **Step 3: Full build and lint verification**
    - `pnpm build` -- zero errors
    - `pnpm check` -- zero errors, zero warnings
    - `pnpm lint` -- zero errors
    - Verify all expected pages exist in dist/:
      - dist/cs/index.html
      - dist/en/index.html
      - dist/cs/produkt/index.html
      - dist/en/product/index.html
      - dist/cs/faq/index.html
      - dist/en/faq/index.html
      - dist/cs/certifikaty/index.html
      - dist/en/certificates/index.html
      - dist/cs/kontakt/index.html
      - dist/en/contact/index.html

    **Step 4: Link integrity check**
    Verify that all internal navigation links resolve to actual pages:
    - Header nav links (5 items x 2 languages = 10 links)
    - Footer nav links (same 10)
    - CTA buttons on homepage and product page link to contact page
    - Language switcher on each page maps to correct alternate-language page
    Grep the built HTML for href values and verify each target exists.

    **Step 5: Count and report**
    Report the total number of translation keys in each language file and the total number of pages built.
  </action>
  <verify>
    1. i18n parity check script outputs "All keys match!"
    2. No raw key fallbacks found in built HTML
    3. `pnpm build`, `pnpm check`, `pnpm lint` all pass with zero errors
    4. All 10 expected HTML pages exist in dist/
    5. No broken internal links (all href targets resolve to existing pages)
  </verify>
  <done>
    Both cs.json and en.json have identical key sets. No translation fallbacks visible in built output. All 10 pages (5 per language) build successfully. All internal navigation links resolve correctly. Build, type-check, and lint all pass clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human content and visual review</name>
  <what-built>
    Complete Phase 2 content across 5 page types in Czech and English:
    1. Homepage -- enhanced with hero, 4 stats, why TC101, industries, CTA band
    2. Product page -- hero, specs table, how-it-works, advantages, comparison
    3. FAQ page -- 4 categories, ~16 questions with accordion
    4. Resources page -- documents/certificates, test results, application examples, application guide
    5. Contact page -- person info, phone, email, address, CTA
    Plus: sticky mobile CTA bar on all pages, full i18n parity
  </what-built>
  <how-to-verify>
    Run `pnpm dev` and check these pages in the browser:

    **Czech pages:**
    1. http://localhost:4321/cs/ -- Homepage: hero visible within 10s? Stats clear? CTA prominent?
    2. http://localhost:4321/cs/produkt/ -- Specs table readable? How-it-works clear? Comparison makes TC101 look compelling?
    3. http://localhost:4321/cs/faq/ -- Accordion works (click to expand)? Questions relevant? Answers substantive?
    4. http://localhost:4321/cs/certifikaty/ -- Documents downloadable? Test summaries useful? Application examples clear? Guide practical?
    5. http://localhost:4321/cs/kontakt/ -- Phone/email clickable? Address clear? Professional appearance?

    **English pages:**
    6. http://localhost:4321/en/ -- Content reads as native English (not translated Czech)?
    7. http://localhost:4321/en/product/ -- Same quality as Czech version?
    8. http://localhost:4321/en/faq/ -- Natural English phrasing?

    **Cross-cutting:**
    9. Language switcher works on every page (click to switch, verify correct URL mapping)
    10. Sticky CTA visible on mobile viewport (resize browser to <768px width, scroll down)
    11. Navigation highlights correct item on each page
    12. Footer contact info visible on every page

    **Content quality check:**
    - Does Czech text sound like a native Czech speaker wrote it? (Not translated from Russian)
    - Does English text sound like a native English speaker wrote it? (Not translated from Czech)
    - Are technical claims backed by specific numbers and certification references?
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- All 10 pages (5 cs + 5 en) build and render correctly
- i18n key parity: cs.json and en.json have identical key sets
- Sticky CTA bar visible on mobile on all pages
- Contact info (phone, email, address) visible in footer on every page AND on dedicated contact page
- All PDF documents accessible via /documents/*.pdf
- FAQ accordion functions with native HTML (no JS)
- No broken navigation links, language switcher works on all pages
- Build, type-check, and lint pass clean
- Human approves content quality and visual appearance
</verification>

<success_criteria>
- CONV-01: Contact info on every page (footer) + dedicated contact page -- COVERED by footer (Phase 1) + new contact page + sticky CTA
- CONV-03: Persistent sticky consultation CTA -- COVERED by StickyCtaBar in BaseLayout on mobile
- QUAL-01: Modern Czech content from scratch -- COVERED by human review of Czech text quality
- QUAL-02: Claims framed around Czech certifications -- COVERED by human review of technical content
- QUAL-03: Native English parity -- COVERED by human review of English text quality
- Full i18n parity between CZ and EN -- COVERED by automated key comparison
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-conversion/02-03-SUMMARY.md`
</output>
