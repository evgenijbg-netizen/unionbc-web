---
phase: 02.3-messaging-rewrite-tone-of-voice
plan: 03
type: execute
wave: 3
depends_on: ["02.3-01", "02.3-02"]
files_modified:
  - src/i18n/cs.json
  - src/i18n/en.json
  - src/components/shared/StickyCtaBar.astro
  - src/pages/cs/kontakt.astro
  - src/pages/en/contact.astro
  - src/pages/cs/index.astro
  - src/pages/en/index.astro
autonomous: true
must_haves:
  truths:
    - "All CTAs across the site say 'Pozadejte o technickou konzultaci' / 'Request technical consultation'"
    - "Sticky CTA bar on mobile uses updated consultation language"
    - "Reference section exists on homepage with structured placeholder"
    - "No marketing superlatives remain in any i18n value across the entire site"
    - "Site builds with 0 errors and all pages render correctly"
    - "CZ and EN i18n files have identical key sets with no orphaned keys"
  artifacts:
    - path: "src/i18n/cs.json"
      provides: "Final harmonized Czech translations"
      contains: "cta.consultation"
    - path: "src/i18n/en.json"
      provides: "Final harmonized English translations"
      contains: "cta.consultation"
    - path: "src/components/shared/StickyCtaBar.astro"
      provides: "Updated sticky CTA with consultation language"
      contains: "cta.consultation"
  key_links:
    - from: "src/components/shared/StickyCtaBar.astro"
      to: "src/i18n/cs.json"
      via: "t(locale, 'cta.consultation')"
      pattern: "cta\\.consultation"
    - from: "src/pages/cs/index.astro"
      to: "src/i18n/cs.json"
      via: "t(locale, 'home.references.*')"
      pattern: "home\\.references"
---

<objective>
Harmonize all CTAs across the entire site to use "technical consultation" language, add a structured reference placeholder section to the homepage, clean up orphaned i18n keys from old sections, and perform a final tone audit to remove any remaining marketing superlatives.

Purpose: Complete the Phase 02.3 messaging transformation by ensuring consistency across every page and CTA. The reference section placeholder signals credibility even before real reference data is available.
Output: Fully harmonized site with consistent CTA language, reference placeholder, clean i18n files, and verified build.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-messaging-rewrite-tone-of-voice/02.3-01-SUMMARY.md
@.planning/phases/02.3-messaging-rewrite-tone-of-voice/02.3-02-SUMMARY.md
@src/i18n/cs.json
@src/i18n/en.json
@src/components/shared/StickyCtaBar.astro
@src/pages/cs/kontakt.astro
@src/pages/en/contact.astro
@src/pages/cs/index.astro
@src/pages/en/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reference placeholder and harmonize all CTAs and i18n cleanup</name>
  <files>src/i18n/cs.json, src/i18n/en.json</files>
  <action>
**1. Add reference section i18n keys:**

Add keys for a structured reference placeholder section on the homepage with prefix `home.references.*`:
- `home.references.title`: CZ "Reference" / EN "References"
- `home.references.subtitle`: CZ "Vyber z realizovanych projektu" / EN "Selected completed projects"
- `home.references.placeholder.title`: CZ "Reference pripravujeme" / EN "References coming soon"
- `home.references.placeholder.description`: CZ "Aktualne pripravujeme podrobne referencni karty z realizovanych projektu. Kazda reference bude obsahovat typ zarizeni, provozni teplotu, tloustku natery a vysledek." / EN native equivalent describing that reference cards will include: equipment type, operating temperature, coating thickness, and result.
- `home.references.placeholder.cta`: CZ "Pozadejte reference na konkretni typ aplikace" / EN "Request references for your specific application type"

**2. Verify and harmonize ALL CTA keys across the site:**

The `cta.consultation` key was already updated in Plan 01 to "Pozadejte o technickou konzultaci" / "Request technical consultation". Verify this key is correct.

Check and update these additional CTA-related keys if they still contain generic "contact us" language:
- `cta.quote`: Change CZ to "Poptat technicke reseni" / EN "Request technical solution" (or keep if it serves a distinct purpose from consultation)
- `contact.page.title`: CZ "Technicka konzultace" / EN "Technical Consultation" -- rewrite from generic "Kontaktujte nas" / "Contact Us"
- `contact.page.subtitle`: CZ rewrite to focus on consultation: "Popistte nam vas projekt a pripravime technicke doporuceni" / EN native equivalent
- `contact.cta.title`: CZ "Technicka konzultace zdarma" / EN "Free Technical Consultation"
- `contact.cta.description`: CZ rewrite to consultation framing / EN native

Do NOT change `contact.meta.title` or `contact.meta.description` -- SEO meta tags need "kontakt/contact" for search discoverability. Only change user-facing page headings.

**3. Tone audit -- scan ALL i18n values and fix superlatives:**

Go through every value in both cs.json and en.json. Remove or replace:
- "extremne" / "extremely" -> use factual description
- "revolucni" / "revolutionary" -> remove
- "nejlepsi" / "best" -> remove or replace with specific
- "jediny" / "only" (when used as superlative) -> remove
- "unikatni" / "unique" -> replace with specific differentiator
- Any unverified percentage claims -> "vypocet na vyzadani" / "calculation available on request"

Specific items to check from current content:
- `product.advantages.lightweight.title` "Extremne lehky" -> if this key still exists and is referenced, tone it down
- Any remaining old keys that use superlatives

**4. Clean up orphaned i18n keys:**

Remove old keys that are no longer referenced by any .astro template after Plans 01 and 02:
- `home.why.time.title`, `home.why.time.description`
- `home.why.money.title`, `home.why.money.description`
- `home.why.proven.title`, `home.why.proven.description`
- `home.why.title`

Check if old `product.advantages.*` keys (the 8 feature-description keys) are still referenced by any component. If AdvantagesList.astro was updated to use `product.benefits.*` prefix in Plan 02, then the old `product.advantages.*` keys are orphaned and should be removed.

**CRITICAL: Before removing any key, verify it is not referenced in any .astro file.** Search for the key string in the codebase. Only remove keys with zero references.

After cleanup, verify cs.json and en.json have identical key sets (same count, same key names).

**5. Update site.description for both languages:**
- `site.description`: CZ "Tenkovrstvy tepelna ochrana prumyslovych zarizeni bez odstavky provozu" / EN native equivalent. This appears in meta tags and should reflect the new problem-first messaging.
  </action>
  <verify>
1. Run `node -e "const cs=require('./src/i18n/cs.json'); const en=require('./src/i18n/en.json'); const csKeys=Object.keys(cs).sort(); const enKeys=Object.keys(en).sort(); if(csKeys.length!==enKeys.length){console.error('COUNT MISMATCH: CS='+csKeys.length+' EN='+enKeys.length)}; csKeys.forEach((k,i)=>{if(k!==enKeys[i])console.error('KEY MISMATCH:',k,'vs',enKeys[i])}); console.log('Total keys:', csKeys.length, '-- Parity check complete');"` -- no mismatches.
2. Run `node -e "const cs=require('./src/i18n/cs.json'); const superlatives=['extremne','revolucni','nejlepsi','jediny','unikatni']; Object.entries(cs).forEach(([k,v])=>{superlatives.forEach(s=>{if(v.toLowerCase().includes(s))console.error('SUPERLATIVE in',k,':',s)})}); console.log('Tone audit complete');"` -- no superlatives found.
3. Verify reference keys exist: `node -e "const cs=require('./src/i18n/cs.json'); ['home.references.title','home.references.placeholder.title','home.references.placeholder.description'].forEach(k=>{if(!cs[k])console.error('MISSING:',k)}); console.log('Reference keys OK');"` -- all present.
  </verify>
  <done>All i18n keys harmonized: CTAs use consultation language, reference placeholder keys exist, orphaned keys removed, no marketing superlatives remain, CS and EN key sets are identical.</done>
</task>

<task type="auto">
  <name>Task 2: Wire reference section into homepage and update StickyCtaBar and contact pages</name>
  <files>src/pages/cs/index.astro, src/pages/en/index.astro, src/components/shared/StickyCtaBar.astro, src/pages/cs/kontakt.astro, src/pages/en/contact.astro</files>
  <action>
**1. Add reference placeholder section to both homepage templates (cs/index.astro and en/index.astro):**

Insert a new section between "Proc UNION B+C" and the final CTA. Structure:

```
<!-- Reference Section (Placeholder) -->
<section class="py-16 px-6 bg-surface-100">
  <div class="max-w-4xl mx-auto text-center">
    <h2 class="text-3xl md:text-4xl font-heading font-bold text-text-primary mb-4">
      {t(locale, "home.references.title")}
    </h2>
    <p class="text-lg text-text-secondary mb-8">
      {t(locale, "home.references.subtitle")}
    </p>
    <!-- Placeholder card -->
    <div class="bg-white border border-surface-200 rounded-lg p-8 max-w-2xl mx-auto">
      <div class="text-accent-400 mb-4">
        <!-- Simple icon: document/clipboard -->
        <svg class="w-12 h-12 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
        </svg>
      </div>
      <h3 class="text-xl font-heading font-semibold text-text-primary mb-3">
        {t(locale, "home.references.placeholder.title")}
      </h3>
      <p class="text-text-secondary mb-6">
        {t(locale, "home.references.placeholder.description")}
      </p>
      <a href={getLocalizedPath("contact", locale)} class="inline-block bg-accent-500 hover:bg-accent-400 text-white font-heading font-semibold px-6 py-2 rounded-sm transition text-sm">
        {t(locale, "home.references.placeholder.cta")}
      </a>
    </div>
  </div>
</section>
```

Both homepage files must have identical HTML structure.

**2. Verify StickyCtaBar.astro uses `cta.consultation` key:**

Read `src/components/shared/StickyCtaBar.astro`. It already uses `t(locale, "cta.consultation")` which was updated in Plan 01 to "Pozadejte o technickou konzultaci" / "Request technical consultation". If the key reference is correct, no changes needed to this file. The CTA text will automatically update from the i18n value change.

If for any reason it uses a different key (e.g., hardcoded text or a different key), update it to use `t(locale, "cta.consultation")`.

**3. Update contact page headings (cs/kontakt.astro and en/contact.astro):**

Read both contact page files. Update the page title and subtitle to use the updated i18n keys from Task 1 (`contact.page.title`, `contact.page.subtitle`). The actual text change happens via i18n -- just verify the template references the correct keys. If the template already uses `t(locale, "contact.page.title")` and `t(locale, "contact.page.subtitle")`, no template changes needed.

Also verify the contact CTA section at the bottom uses `contact.cta.title` and `contact.cta.description` keys.

**4. Final build verification:**

Run `pnpm build` to verify 0 errors across all pages. This is the critical final check that all i18n keys referenced by templates exist in both JSON files.
  </action>
  <verify>
1. `pnpm build` -- must succeed with 0 errors (this validates ALL i18n key references across ALL pages).
2. Check homepage reference section: search both index.astro files for "home.references" -- must be present.
3. Check StickyCtaBar uses consultation key: search for "cta.consultation" in StickyCtaBar.astro.
4. Run dev server and manually verify:
   - http://localhost:4321/unionbc-web/cs/ -- reference placeholder section visible between "Proc UNION B+C" and CTA
   - http://localhost:4321/unionbc-web/en/ -- same in English
   - Sticky CTA bar on mobile (resize to <768px) shows "Pozadejte o technickou konzultaci" / "Request technical consultation"
   - Contact page heading shows consultation language
  </verify>
  <done>Reference placeholder section visible on both homepage versions. All CTAs across the site consistently use "technical consultation" language (homepage, product page, sticky bar, contact page). Site builds with 0 errors. All pages render correctly in both CZ and EN.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with 0 errors -- all i18n references valid
2. CZ and EN i18n files have identical key sets (zero orphaned, zero missing)
3. No marketing superlatives in any i18n value
4. Reference placeholder section renders on both homepage versions
5. All CTAs site-wide use "technical consultation" language
6. Contact page heading uses consultation framing
7. Sticky CTA bar updated
8. Site renders correctly on all pages in both languages
</verification>

<success_criteria>
Phase 02.3 is complete: site communicates as a technical partner, not a product catalog. Homepage leads with problems, product page shows customer outcomes, cooperation workflow is visible, references section exists as placeholder, all CTAs are harmonized, tone is factual throughout. Build passes with 0 errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-messaging-rewrite-tone-of-voice/02.3-03-SUMMARY.md`
</output>
