---
phase: 02.3-messaging-rewrite-tone-of-voice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/cs.json
  - src/i18n/en.json
  - src/pages/cs/index.astro
  - src/pages/en/index.astro
autonomous: true
must_haves:
  truths:
    - "Homepage hero leads with problem/solution not product description"
    - "Homepage includes 'Jaky problem resime' section with industry pain points"
    - "Homepage includes 'Pro koho je reseni urceno' section with target industries"
    - "Homepage includes 'Proc UNION B+C' section with differentiators"
    - "Homepage CTA says 'Pozadejte o technickou konzultaci' in CZ and 'Request technical consultation' in EN"
    - "No marketing superlatives in homepage content"
  artifacts:
    - path: "src/i18n/cs.json"
      provides: "Czech homepage messaging keys"
      contains: "home.hero.title"
    - path: "src/i18n/en.json"
      provides: "English homepage messaging keys"
      contains: "home.hero.title"
    - path: "src/pages/cs/index.astro"
      provides: "Czech homepage with new sections"
      contains: "home.problem"
    - path: "src/pages/en/index.astro"
      provides: "English homepage with new sections"
      contains: "home.problem"
  key_links:
    - from: "src/pages/cs/index.astro"
      to: "src/i18n/cs.json"
      via: "t(locale, key) calls"
      pattern: "t\\(locale.*home\\."
    - from: "src/pages/en/index.astro"
      to: "src/i18n/en.json"
      via: "t(locale, key) calls"
      pattern: "t\\(locale.*home\\."
---

<objective>
Rewrite the homepage to lead with problem-first messaging instead of product description. Add new sections: "Jaky problem resime" (pain points), restructured "Nase reseni" (solution benefits), "Pro koho je reseni urceno" (target industries with descriptions), and "Proc UNION B+C" (differentiators). Rewrite all CTAs to use "technical consultation" language.

Purpose: Transform the homepage from a product catalog opening ("TC101 je keramicky nater") to a technical partner message ("Snizujeme tepelne ztraty bez odstavky"). This is the core messaging shift of Phase 02.3.
Output: Rewritten homepage in both CZ and EN with problem-first hero, 4 new/restructured sections, and updated CTA language.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/i18n/cs.json
@src/i18n/en.json
@src/pages/cs/index.astro
@src/pages/en/index.astro
@src/i18n/utils.ts
@src/i18n/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite homepage i18n keys for problem-first messaging</name>
  <files>src/i18n/cs.json, src/i18n/en.json</files>
  <action>
Rewrite and add homepage i18n keys in both cs.json and en.json. CZ texts are based on "navrh textu.txt" content provided below. EN texts must be written natively (not translated from CZ) per QUAL-03.

**HERO section -- rewrite existing keys:**
- `home.hero.title`: CZ "Tenkovrstva tepelna ochrana pro prumysl a energetiku" / EN write native equivalent (not literal translation)
- `home.hero.subtitle`: CZ "Snizujeme tepelne ztraty technologickych zarizeni bez odstavky provozu." / EN native equivalent
- `home.hero.description`: Remove current product-description text. Replace with short problem-framing sentence about industrial heat loss challenges.
- `home.hero.valueProp`: Remove or repurpose -- the hero now leads with the problem, not a tagline.
- Keep `home.hero.learnMore` as-is.

**NEW "Jaky problem resime" section -- add keys with prefix `home.problem.`:**
- `home.problem.title`: CZ "Jaky problem resime" / EN "The Problem We Solve"
- `home.problem.subtitle`: CZ brief intro sentence about industrial heat loss challenges
- 4 bullet points as `home.problem.item1` through `home.problem.item4`:
  - CZ from navrh textu: (1) heat losses on uninsulated pipes/tanks, (2) burn risk for personnel, (3) CUI under conventional insulation, (4) conventional insulation impractical on complex geometries
  - EN: native equivalents, same factual content

**RESTRUCTURE "Nase reseni" section -- replace current `home.why.*` keys:**
- `home.solution.title`: CZ "Nase reseni" / EN "Our Solution"
- `home.solution.subtitle`: CZ brief intro about TC101 as thin-layer ceramic coating
- 5 benefit items as `home.solution.item1.title` / `home.solution.item1.description` through item5:
  - CZ from navrh textu: (1) surface temp reduction without downtime, (2) burn protection, (3) CUI prevention, (4) application on complex geometries, (5) visual inspection possible
  - EN: native equivalents
- Add 4 evaluation criteria as `home.solution.criteria1` through `criteria4`:
  - CZ from navrh textu: technical consultation, on-site assessment, custom proposal, long-term support
  - EN: native equivalents

**RESTRUCTURE "Pro koho" section -- replace current `home.industries.*` keys:**
- `home.target.title`: CZ "Pro koho je reseni urceno" / EN "Who This Solution Is For"
- 7 industry items with title+description: `home.target.energy.title`, `home.target.energy.description`, etc.
  - CZ from navrh textu: energetika, petrochemie, potravinarstvi, papirenstvi, stavebnictvi, lodni prumysl, udrzba prumyslovych arealu
  - EN: native equivalents with industry-specific phrasing
- Keep existing industry icon/pill UI but enhance to show descriptions

**NEW "Proc UNION B+C" section -- add keys with prefix `home.whyus.`:**
- `home.whyus.title`: CZ "Proc UNION B+C" / EN "Why UNION B+C"
- 4 differentiators as `home.whyus.item1.title` / `home.whyus.item1.description` through item4:
  - CZ from navrh textu: (1) zkusenosti (years in market), (2) konzultacni pristup, (3) individualni reseni, (4) dlouhodoba podpora
  - EN: native equivalents
- For founding year / years in market: use existing verified numbers from current site (company founded info). If no founding year is available in current data, use general phrasing like "years of experience" without specific numbers.

**REWRITE CTA section:**
- `home.cta.title`: CZ "Technicka konzultace zdarma" / EN native equivalent
- `home.cta.description`: CZ text about what consultation includes
- `home.cta.button`: CZ "Pozadejte o technickou konzultaci" / EN "Request technical consultation"

**REWRITE global CTA key:**
- `cta.consultation`: CZ "Pozadejte o technickou konzultaci" / EN "Request technical consultation"

**REMOVE old keys that are no longer used** (home.why.time.*, home.why.money.*, home.why.proven.*) -- do NOT remove yet, just leave them. They will be cleaned up in Plan 03.

**TONE RULES (apply to ALL new/modified keys):**
- No superlatives ("nejlepsi", "jediny", "revolucni", "best", "revolutionary")
- Factual, technical, solution-oriented
- Where specific numbers are unavailable, use "vypocet na vyzadani" / "calculation available on request"
- Use existing verified temperatures and specs from current site (do not invent numbers)

NOTE: Czech text uses proper diacritics (hacky, carky). The key names above are simplified -- actual values in JSON must have full Czech diacritics.
  </action>
  <verify>
Run `node -e "const cs=require('./src/i18n/cs.json'); const en=require('./src/i18n/en.json'); const keys=['home.problem.title','home.solution.title','home.target.title','home.whyus.title','home.cta.button','cta.consultation']; keys.forEach(k => { if(!cs[k]) console.error('MISSING CS:', k); if(!en[k]) console.error('MISSING EN:', k); }); console.log('CS keys:', Object.keys(cs).length, 'EN keys:', Object.keys(en).length); if(Object.keys(cs).length !== Object.keys(en).length) console.error('KEY COUNT MISMATCH');"` -- all keys present, counts match.
  </verify>
  <done>Both cs.json and en.json contain all new homepage keys (home.problem.*, home.solution.*, home.target.*, home.whyus.*, updated home.cta.*, updated cta.consultation). CZ text uses diacritics. EN text is native-quality. Key counts match between files. No marketing superlatives in any new text.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild homepage template with new section structure</name>
  <files>src/pages/cs/index.astro, src/pages/en/index.astro</files>
  <action>
Rewrite both homepage templates (cs/index.astro and en/index.astro -- they are structurally identical, just different locale). Both files must remain structurally identical (same HTML, same t() calls, different locale resolution).

**New section order (top to bottom):**

1. **Hero** -- same structure but use new i18n keys. Keep the two-button layout (primary CTA + "Learn more"). Primary CTA text: `t(locale, "cta.consultation")`. Keep product image section below hero as-is.

2. **Key Stats** -- keep existing 4-card stats section unchanged (temp range, visual inspection, application, certified). This provides quick credibility.

3. **Product Image** -- keep existing TC101 bucket image section as-is.

4. **"Jaky problem resime"** (NEW) -- section with bg-surface-100. `h2` title from `home.problem.title`. Subtitle paragraph from `home.problem.subtitle`. Then a 2x2 grid (md:grid-cols-2) of 4 cards, each with an icon placeholder (use simple SVG icons -- exclamation-triangle for problems), title from `home.problem.item{N}.title` if you split title/desc, OR just a `<p>` bullet with `home.problem.item{N}` if stored as single strings. Use the card style from existing site: `bg-surface-200 border border-surface-200 rounded-lg p-6`.

5. **"Nase reseni"** (RESTRUCTURED from "Why TC101") -- section with white bg. Title from `home.solution.title`. Subtitle from `home.solution.subtitle`. 5 benefit items in a responsive grid (md:grid-cols-2 lg:grid-cols-3 for first 3, then md:grid-cols-2 for last 2, or just a single 2-col grid for all 5). Each item: icon (checkmark SVG), title, description. Below the benefits grid, add a sub-section with 4 evaluation criteria as a horizontal row of small cards or a simple 4-column grid.

6. **"Pro koho je reseni urceno"** (RESTRUCTURED from "Industries") -- section with bg-surface-100. Title from `home.target.title`. Instead of simple pills, render industry items as cards with title + description. Use a responsive grid (md:grid-cols-2 lg:grid-cols-3). Each card: `bg-white border border-surface-200 rounded-lg p-5`, h3 title, p description.

7. **"Proc UNION B+C"** (NEW) -- section with white bg. Title from `home.whyus.title`. 4 differentiators in 2x2 grid (md:grid-cols-2). Each item: left border accent (border-l-4 border-accent-500), title, description. This creates visual distinction from other card sections.

8. **CTA** -- same structure as current but with updated i18n keys. Title from `home.cta.title`, description from `home.cta.description`, button from `home.cta.button`.

**Implementation rules:**
- Both cs/index.astro and en/index.astro must have IDENTICAL HTML structure -- only the imports and `const locale` differ.
- Keep existing imports (Image, getLocalizedPath, getLocaleFromUrl, t, BaseLayout, productImage).
- Keep Tailwind classes consistent with existing design system (surface-*, accent-*, text-text-primary, text-text-secondary, font-heading).
- All text comes from t() calls, never hardcoded.
- Remove the old `whyFeatures` and `industries` arrays, replace with new arrays matching the new i18n key structure.
  </action>
  <verify>
Run `pnpm build` from project root -- must complete with 0 errors. Then visually verify structure: `pnpm dev` and check http://localhost:4321/unionbc-web/cs/ and http://localhost:4321/unionbc-web/en/ both render all 8 sections without missing text or layout issues.
  </verify>
  <done>Both homepage templates render all 8 sections in correct order. Hero leads with problem-first messaging. "Jaky problem resime", "Pro koho", and "Proc UNION B+C" sections are visible. CTA button says "Pozadejte o technickou konzultaci" / "Request technical consultation". Site builds with 0 errors.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with 0 errors
2. Homepage CZ hero h1 contains problem-oriented text, not "TEMP-COAT TC101" as product name
3. Homepage has sections: problem, solution, target industries, why UNION B+C, CTA
4. All text rendered via t() calls, no hardcoded strings
5. CZ and EN key counts match in i18n JSON files
6. No marketing superlatives in any new i18n values
</verification>

<success_criteria>
Homepage communicates as a technical partner solving problems, not a product catalog. Hero leads with "Snizujeme tepelne ztraty" messaging. All 4 new/restructured sections render correctly in both languages. CTAs use "technical consultation" language. Build passes with 0 errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-messaging-rewrite-tone-of-voice/02.3-01-SUMMARY.md`
</output>
