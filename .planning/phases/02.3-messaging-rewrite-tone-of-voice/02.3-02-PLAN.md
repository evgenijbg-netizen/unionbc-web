---
phase: 02.3-messaging-rewrite-tone-of-voice
plan: 02
type: execute
wave: 2
depends_on: ["02.3-01"]
files_modified:
  - src/i18n/cs.json
  - src/i18n/en.json
  - src/pages/cs/produkt.astro
  - src/pages/en/product.astro
  - src/components/product/AdvantagesList.astro
  - src/components/product/HeroSection.astro
  - src/components/product/CooperationWorkflow.astro
autonomous: true
must_haves:
  truths:
    - "Product page benefits are rewritten as customer outcomes not feature descriptions"
    - "Product page includes 'Postup spoluprace' 4-step cooperation workflow section"
    - "Product page hero describes the product as a solution, not just coating specs"
    - "Product page CTA says 'Pozadejte o technickou konzultaci' / 'Request technical consultation'"
    - "Product page content is factual with no marketing superlatives"
  artifacts:
    - path: "src/components/product/CooperationWorkflow.astro"
      provides: "4-step cooperation workflow component"
      min_lines: 30
    - path: "src/components/product/AdvantagesList.astro"
      provides: "Rewritten advantages as customer outcomes"
      contains: "product.benefits"
    - path: "src/components/product/HeroSection.astro"
      provides: "Solution-oriented product hero"
      contains: "product.hero"
  key_links:
    - from: "src/components/product/CooperationWorkflow.astro"
      to: "src/i18n/cs.json"
      via: "t(locale, key) calls"
      pattern: "t\\(locale.*product\\.workflow"
    - from: "src/pages/cs/produkt.astro"
      to: "src/components/product/CooperationWorkflow.astro"
      via: "component import and render"
      pattern: "CooperationWorkflow"
---

<objective>
Rewrite the product page to frame benefits as customer outcomes and add a cooperation workflow section. Update the product hero to position TC101 as a solution (not just describe the coating), rewrite advantages from feature-descriptions to outcome-statements, and add a new "Postup spoluprace" (cooperation workflow) 4-step section.

Purpose: Complete the product page transformation from feature catalog to solution narrative. Visitors see what TC101 solves for THEM (safety, no downtime, corrosion protection) and understand the cooperation process (consultation -> assessment -> proposal -> realization).
Output: Rewritten product page with outcome-oriented benefits, cooperation workflow component, and updated CTA language.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-messaging-rewrite-tone-of-voice/02.3-01-SUMMARY.md
@src/i18n/cs.json
@src/i18n/en.json
@src/pages/cs/produkt.astro
@src/pages/en/product.astro
@src/components/product/AdvantagesList.astro
@src/components/product/HeroSection.astro
@src/i18n/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite product page i18n keys and add cooperation workflow keys</name>
  <files>src/i18n/cs.json, src/i18n/en.json</files>
  <action>
Update product page i18n keys in both cs.json and en.json. CZ texts based on "navrh textu.txt". EN texts written natively (not machine-translated) per QUAL-03.

**PRODUCT HERO -- update existing keys:**
- `product.hero.subtitle`: CZ "Tenkovrstva tepelna ochrana pro prumyslova zarizeni" / EN native equivalent. Frame as solution, not just "coating".
- `product.hero.description`: CZ rewrite from navrh textu -- describe what TC101 is (ceramic coating with microspheres), what surfaces it works on (5 types: ocel, nerez, beton, drevo, plasty), how it's applied. Keep factual.
- `product.hero.cta`: CZ "Pozadejte o technickou konzultaci" / EN "Request technical consultation"

**PRODUCT CHARACTERISTICS -- add new section keys `product.characteristics.*`:**
- `product.characteristics.title`: CZ "Charakteristika produktu" / EN "Product Characteristics"
- `product.characteristics.description`: CZ from navrh textu -- TC101 is a multi-component water-based coating with ceramic microspheres, developed for protection of industrial equipment. List the 5 surface types it works on.
- `product.characteristics.surfaces`: CZ comma-separated list of surfaces / EN equivalent

**PRODUCT BENEFITS -- rewrite existing `product.advantages.*` keys to outcome-oriented:**

Replace the current 8 feature-descriptions with 5 customer-outcome benefits. Use new key prefix `product.benefits.*` (keep old `product.advantages.*` keys for now, they'll be cleaned up in Plan 03):

- `product.benefits.title`: CZ "Hlavni prinosy" / EN "Key Benefits"
- `product.benefits.safety.title`: CZ "Bezpecnost personalu" / EN "Personnel Safety"
- `product.benefits.safety.description`: CZ outcome: reduces surface temp to safe levels, personnel protected from burns without production shutdown / EN native
- `product.benefits.noDowntime.title`: CZ "Aplikace bez odstavky" / EN "Application Without Shutdown"
- `product.benefits.noDowntime.description`: CZ outcome: apply during operation, no scaffolding, no disassembly, hours not days / EN native
- `product.benefits.corrosion.title`: CZ "Ochrana proti korozi" / EN "Corrosion Protection"
- `product.benefits.corrosion.description`: CZ outcome: breathable layer prevents CUI, surface visible for inspection / EN native
- `product.benefits.versatility.title`: CZ "Univerzalnost pouziti" / EN "Versatile Application"
- `product.benefits.versatility.description`: CZ outcome: works on pipes, tanks, complex geometries, -60 to +260C / EN native
- `product.benefits.savings.title`: CZ "Ekonomicka efektivita" / EN "Cost Effectiveness"
- `product.benefits.savings.description`: CZ outcome: lower material cost, lower labor cost, long lifespan, "presny vypocet na vyzadani" / EN native with "detailed calculation available on request"

**TYPICAL APPLICATIONS -- add new keys `product.usecases.*`:**
- `product.usecases.title`: CZ "Typicke oblasti pouziti" / EN "Typical Applications"
- 6 use cases as `product.usecases.item1` through `product.usecases.item6`:
  - CZ from navrh textu: potrubi pary a horkych medii, nadrze a reaktory, tepelne vymeniky, kominy a kourovody, prumyslove haly (strechy/steny), chladici systemy
  - EN: native equivalents

**COOPERATION WORKFLOW -- add new keys `product.workflow.*`:**
- `product.workflow.title`: CZ "Postup spoluprace" / EN "How We Work Together"
- `product.workflow.subtitle`: CZ "Od prvniho kontaktu k realizaci" / EN native
- 4 steps as `product.workflow.step1.title` / `product.workflow.step1.description` through step4:
  - Step 1: CZ "Technicka konzultace" -- describe: you describe the problem, we assess requirements / EN native
  - Step 2: CZ "Posouzeni na miste" -- describe: on-site assessment, measurements, conditions / EN native
  - Step 3: CZ "Navrh reseni" -- describe: custom proposal with thickness, consumption, timeline / EN native
  - Step 4: CZ "Realizace a podpora" -- describe: professional application or supervision, follow-up / EN native

**PRODUCT CTA -- add/update keys:**
- `product.cta.title`: CZ "Poptavka technickeho reseni" / EN "Request a Technical Solution"
- `product.cta.description`: CZ from navrh textu: describe your situation, we prepare a proposal
- `product.cta.button`: CZ "Pozadejte o technickou konzultaci" / EN "Request technical consultation"

**TECHNICAL INFO note -- add key:**
- `product.techInfo.note`: CZ "Presne parametry zavisi na konkretni aplikaci. Pozadejte o technickou konzultaci pro vypocet na miru." / EN native equivalent. This replaces any unverifiable savings percentages.

**TONE RULES:** Same as Plan 01 -- no superlatives, factual, technical. Use "vypocet na vyzadani" for unverifiable savings claims. Use existing verified numbers from current site for temperatures and specs.
  </action>
  <verify>
Run `node -e "const cs=require('./src/i18n/cs.json'); const en=require('./src/i18n/en.json'); const keys=['product.benefits.title','product.benefits.safety.title','product.workflow.title','product.workflow.step1.title','product.workflow.step4.title','product.cta.button','product.usecases.title']; keys.forEach(k => { if(!cs[k]) console.error('MISSING CS:', k); if(!en[k]) console.error('MISSING EN:', k); }); console.log('CS keys:', Object.keys(cs).length, 'EN keys:', Object.keys(en).length); if(Object.keys(cs).length !== Object.keys(en).length) console.error('KEY COUNT MISMATCH');"` -- all keys present, counts match.
  </verify>
  <done>Both cs.json and en.json contain all new product page keys (product.benefits.*, product.workflow.*, product.usecases.*, product.cta.*, product.characteristics.*, product.techInfo.note). Benefits are framed as customer outcomes. Workflow has 4 steps. Key counts match. No marketing superlatives.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild product page template with new components and section structure</name>
  <files>src/components/product/CooperationWorkflow.astro, src/components/product/AdvantagesList.astro, src/components/product/HeroSection.astro, src/pages/cs/produkt.astro, src/pages/en/product.astro</files>
  <action>
**1. Create new CooperationWorkflow.astro component:**

Create `src/components/product/CooperationWorkflow.astro` following the existing component pattern (accepts `locale: Locale` prop, uses `t()` for all text).

Layout: Section with bg-surface-100, py-16 px-6. Title h2 from `product.workflow.title`. Subtitle paragraph from `product.workflow.subtitle`. Then 4 steps in a horizontal timeline layout:
- On desktop (md+): 4-column grid with numbered circles (1-4) connected by a visual line/arrow
- On mobile: vertical stack with numbered circles on the left

Each step: numbered circle (bg-accent-500 text-white w-10 h-10 rounded-full, number inside), title from `product.workflow.step{N}.title`, description from `product.workflow.step{N}.description`.

Use existing Tailwind design tokens. Import SectionHeading from shared if appropriate, or use plain h2 matching other sections.

The step numbers should be rendered as `{i + 1}` in a map over ["step1", "step2", "step3", "step4"].

**2. Update AdvantagesList.astro to use new benefit keys:**

Modify `src/components/product/AdvantagesList.astro`:
- Change the advantages array from the old 8 feature keys to 5 outcome keys: `["safety", "noDowntime", "corrosion", "versatility", "savings"]`
- Update the t() key prefix from `product.advantages.${adv}` to `product.benefits.${adv}`
- Update the section title to use `product.benefits.title` instead of `product.advantages.title`
- Keep the existing card layout (bg-surface-200 with checkmark icons) but adjust grid for 5 items: use `md:grid-cols-2 lg:grid-cols-3` so it wraps naturally (3+2 on large, 2+2+1 on medium, stacked on mobile)

**3. Update HeroSection.astro:**

The hero already uses t() calls for all content, so the i18n key updates from Task 1 will automatically change the displayed text. However, verify the CTA button uses `product.hero.cta` key (it already does from reading the code). No structural changes needed to HeroSection.astro unless the subtitle or description key names changed -- they didn't (same keys, new values).

**4. Update product page templates (cs/produkt.astro and en/product.astro):**

Add the new CooperationWorkflow component and a product CTA section. The new section order:

```
<HeroSection />
<SpecsTable />
<HowItWorks />
<AdvantagesList />       (now using new benefit keys)
<CooperationWorkflow />  (NEW)
<ComparisonTable />
<!-- Product CTA section (inline, not a component) -->
<section> ... product.cta.title, product.cta.description, product.cta.button ... </section>
```

For the Product CTA section at bottom: use the same CTA pattern as homepage (bg-surface-100, centered text, accent button linking to contact page). Use keys `product.cta.title`, `product.cta.description`, `product.cta.button`.

Both cs/produkt.astro and en/product.astro must have identical structure. Import CooperationWorkflow and pass locale prop.

**Do NOT add product.characteristics or product.usecases sections as visible page sections in this task** -- those keys are prepared for future use or for enriching existing sections. The current specs table and how-it-works already cover this content. The i18n keys exist for completeness and can be wired in future phases if needed.
  </action>
  <verify>
Run `pnpm build` -- must complete with 0 errors. Check that `src/components/product/CooperationWorkflow.astro` exists and has >30 lines. Run `pnpm dev` and verify:
- http://localhost:4321/unionbc-web/cs/produkt/ shows 4-step workflow section
- http://localhost:4321/unionbc-web/en/product/ shows same workflow section in English
- Benefits section shows 5 outcome-oriented items instead of 8 feature items
- Product CTA at bottom says "Pozadejte o technickou konzultaci" / "Request technical consultation"
  </verify>
  <done>Product page renders with: solution-oriented hero, outcome-based benefits (5 items), cooperation workflow (4 steps), product CTA with consultation language. CooperationWorkflow.astro component exists and renders correctly. Both CZ and EN versions work. Site builds with 0 errors.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with 0 errors
2. Product page benefits section shows 5 customer outcomes, not 8 feature descriptions
3. Cooperation workflow section visible with 4 numbered steps
4. Product page CTA uses "technical consultation" language
5. All text from t() calls, no hardcoded strings
6. CZ and EN key counts match
7. CooperationWorkflow.astro follows existing component pattern (locale prop, t() calls)
</verification>

<success_criteria>
Product page communicates customer outcomes (safety, no downtime, corrosion protection) not product features. Cooperation workflow shows clear 4-step process. All CTAs use "technical consultation" language. Build passes with 0 errors in both languages.
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-messaging-rewrite-tone-of-voice/02.3-02-SUMMARY.md`
</output>
