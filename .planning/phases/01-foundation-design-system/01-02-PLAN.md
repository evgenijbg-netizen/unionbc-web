---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/i18n/utils.ts
  - src/i18n/routes.ts
  - src/i18n/cs.json
  - src/i18n/en.json
  - src/layouts/BaseLayout.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/LanguageSwitcher.astro
  - src/pages/index.astro
  - src/pages/cs/index.astro
  - src/pages/en/index.astro
autonomous: true

must_haves:
  truths:
    - "Visitor sees a Czech homepage at /cs/ with header navigation, footer with contact info, and industrial dark design"
    - "Visitor sees an English homepage at /en/ with the same layout but English labels"
    - "Visitor can click the language switcher in the header to toggle between /cs/ and /en/"
    - "Every page has hreflang tags for cs, en, and x-default in the HTML head"
    - "Visiting / redirects to /cs/"
    - "Layout is responsive -- no horizontal scroll at 375px, 768px, or 1440px viewport widths"
  artifacts:
    - path: "src/i18n/utils.ts"
      provides: "t() translation helper, getLocaleFromUrl(), locale types"
      exports: ["t", "getLocaleFromUrl", "locales", "defaultLocale", "Locale"]
    - path: "src/i18n/routes.ts"
      provides: "Route slug mapping between languages, getLocalizedPath(), getAlternatePath()"
      exports: ["routeMap", "getLocalizedPath", "getAlternatePath"]
    - path: "src/i18n/cs.json"
      provides: "Czech translation strings for navigation, footer, site metadata"
      min_lines: 10
    - path: "src/i18n/en.json"
      provides: "English translation strings for navigation, footer, site metadata"
      min_lines: 10
    - path: "src/layouts/BaseLayout.astro"
      provides: "Shared page shell with html lang, hreflang tags, header, footer, design system"
      min_lines: 40
    - path: "src/components/Header.astro"
      provides: "Site header with navigation links and language switcher"
      min_lines: 20
    - path: "src/components/Footer.astro"
      provides: "Site footer with company contact info"
      min_lines: 15
    - path: "src/components/LanguageSwitcher.astro"
      provides: "Language toggle link using getAlternatePath()"
      min_lines: 5
    - path: "src/pages/cs/index.astro"
      provides: "Czech homepage placeholder using BaseLayout"
      contains: "BaseLayout"
    - path: "src/pages/en/index.astro"
      provides: "English homepage placeholder using BaseLayout"
      contains: "BaseLayout"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/i18n/utils.ts"
      via: "import and call t(), getLocaleFromUrl()"
      pattern: "import.*from.*i18n/utils"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/i18n/routes.ts"
      via: "import getAlternatePath for hreflang generation"
      pattern: "import.*from.*i18n/routes"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "CSS import in frontmatter"
      pattern: "import.*styles/global\\.css"
    - from: "src/components/Header.astro"
      to: "src/i18n/utils.ts"
      via: "t() for navigation labels"
      pattern: "t\\(locale"
    - from: "src/components/LanguageSwitcher.astro"
      to: "src/i18n/routes.ts"
      via: "getAlternatePath() for language toggle URL"
      pattern: "getAlternatePath"
    - from: "src/components/Footer.astro"
      to: "src/i18n/utils.ts"
      via: "t() for footer text"
      pattern: "t\\(locale"
    - from: "src/pages/cs/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout wrapper"
      pattern: "BaseLayout"
    - from: "src/pages/en/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout wrapper"
      pattern: "BaseLayout"
---

<objective>
Build the i18n translation system, shared layout shell (header with navigation + language switcher, footer with contact info), and bilingual homepage placeholders. After this plan, the site has a navigable bilingual shell with industrial design.

Purpose: This is the functional core of Phase 1 -- the layout and i18n system that every page in every future phase will use. Getting the i18n architecture right now prevents a partial rewrite later (locked decision).
Output: Working bilingual site shell with header, footer, language switching, hreflang tags, and placeholder homepages.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n translation system with type-safe utilities</name>
  <files>
    src/i18n/utils.ts
    src/i18n/routes.ts
    src/i18n/cs.json
    src/i18n/en.json
  </files>
  <action>
    1. Create `src/i18n/cs.json` with Czech UI strings. Use flat dot-separated keys. Include:
       - site.title: "TEMP-COAT TC101 | Union B+C"
       - site.description: "Tekuta keramicka tepelna izolace pro prumysl a stavebnictvi" (use proper Czech diacritics: "Tekutá keramická tepelná izolace pro průmysl a stavebnictví")
       - nav.home: "Domů"
       - nav.product: "Produkt TC101"
       - nav.faq: "FAQ"
       - nav.certificates: "Certifikáty"
       - nav.contact: "Kontakt"
       - nav.language: "English" (what the switcher shows -- the OTHER language name)
       - footer.company: "Union B+C s.r.o."
       - footer.address: "Jiskrova 1566, Brandýs nad Labem, 250 01"
       - footer.phone: "+420 777 832 348"
       - footer.email: "info@unionbc.cz"
       - footer.copyright: "Union B+C s.r.o. Všechna práva vyhrazena."
       - cta.consultation: "Nezávazná konzultace"
       - cta.quote: "Poptávka"
       - home.hero.title: "TEMP-COAT TC101"
       - home.hero.subtitle: "Tekutá keramická tepelná izolace"
       - home.hero.description: "Průmyslová tepelná izolace nové generace. Nanáší se jako nátěr, izoluje jako centimetry klasické izolace."

    2. Create `src/i18n/en.json` with English UI strings, same key structure:
       - site.title: "TEMP-COAT TC101 | Union B+C"
       - site.description: "Liquid ceramic thermal insulation for industry and construction"
       - nav.home: "Home"
       - nav.product: "Product TC101"
       - nav.faq: "FAQ"
       - nav.certificates: "Certificates"
       - nav.contact: "Contact"
       - nav.language: "Česky" (the OTHER language name)
       - footer.company: "Union B+C s.r.o."
       - footer.address: "Jiskrova 1566, Brandys nad Labem, 250 01"
       - footer.phone: "+420 777 832 348"
       - footer.email: "info@unionbc.cz"
       - footer.copyright: "Union B+C s.r.o. All rights reserved."
       - cta.consultation: "Free consultation"
       - cta.quote: "Request a quote"
       - home.hero.title: "TEMP-COAT TC101"
       - home.hero.subtitle: "Liquid ceramic thermal insulation"
       - home.hero.description: "Next-generation industrial thermal insulation. Applied like paint, insulates like inches of conventional material."

    3. Create `src/i18n/utils.ts` following the research pattern EXACTLY:
       - Import cs.json and en.json
       - Export `translations` const, `Locale` type, `locales` array, `defaultLocale`
       - Export `t(locale, key)` function: splits key by ".", walks the object, returns string or key as fallback
       - Export `getLocaleFromUrl(url)`: extracts locale from URL pathname first segment
       - Use proper TypeScript types throughout

    4. Create `src/i18n/routes.ts` following the research pattern EXACTLY:
       - Import Locale type from utils
       - Export `routeMap`: maps logical page names to localized slugs for each locale
         - cs: { home: "", product: "produkt", faq: "faq", certificates: "certifikaty", contact: "kontakt" }
         - en: { home: "", product: "product", faq: "faq", certificates: "certificates", contact: "contact" }
       - Export `getLocalizedPath(page, locale)`: returns `/${locale}/${slug}/` or `/${locale}/` for home
       - Export `getAlternatePath(currentPath, targetLocale)`: reverse-looks up the current slug to find the logical page name, then returns the target locale's path. This powers the language switcher and hreflang tags.

    IMPORTANT: Every visible string in components MUST use t(locale, "key"). No hardcoded Czech or English text in .astro files. The route slugs in routeMap MUST match the actual page file names that will be created (page files are created in Task 2 and future phases).
  </action>
  <verify>
    Create a quick TypeScript check: `pnpm astro check` should pass without type errors in the i18n files.
    Manually verify: `t("cs", "nav.home")` would return "Domů" and `t("en", "nav.home")` would return "Home".
    Verify `getAlternatePath("/cs/produkt/", "en")` would return "/en/product/".
    Verify `getAlternatePath("/en/", "cs")` would return "/cs/".
  </verify>
  <done>
    Four i18n files exist (utils.ts, routes.ts, cs.json, en.json). t() function resolves dot-separated keys to translated strings. getLocaleFromUrl() extracts locale from URL. getAlternatePath() correctly maps between Czech and English URL slugs. TypeScript types are correct.
  </done>
</task>

<task type="auto">
  <name>Task 2: Layout shell, header, footer, language switcher, and bilingual homepages</name>
  <files>
    src/layouts/BaseLayout.astro
    src/components/Header.astro
    src/components/Footer.astro
    src/components/LanguageSwitcher.astro
    src/pages/index.astro
    src/pages/cs/index.astro
    src/pages/en/index.astro
  </files>
  <action>
    1. Create `src/components/LanguageSwitcher.astro`:
       - Accepts `locale` and `pageKey` (optional) props
       - Uses getAlternatePath() to compute the URL for the other language
       - Uses t(locale, "nav.language") for the link text (shows "English" when on Czech, "Česky" when on English)
       - Renders as a simple `<a>` tag with appropriate styling (text-text-secondary hover:text-accent-400 transition)

    2. Create `src/components/Header.astro`:
       - Accepts `locale` and `pageKey` (optional) props
       - Contains the site logo/name: "TEMP-COAT" text in font-heading, linked to the locale's homepage
       - Horizontal navigation with links to: Home, Product TC101, FAQ, Certificates, Contact
       - Each nav link uses `getLocalizedPath(pageName, locale)` for the href and `t(locale, "nav.{pageName}")` for the label
       - LanguageSwitcher component on the right
       - Mobile responsive: On screens below md (768px), navigation collapses. Use a simple approach: a hamburger button that toggles a mobile menu via a small inline `<script>` (vanilla JS, no framework needed). The script toggles a CSS class `hidden` on the mobile nav container.
       - Styling: bg-surface-800, border-b border-surface-700, sticky top-0 z-50, py-4 px-6. Nav links: text-text-secondary hover:text-text-primary transition. Active page link: text-accent-400.
       - Use a `<nav>` element with `aria-label` for accessibility.

    3. Create `src/components/Footer.astro`:
       - Accepts `locale` prop
       - Three-column layout on desktop (lg), stacking on mobile:
         - Column 1: Company info -- "Union B+C s.r.o.", address (from translation)
         - Column 2: Contact -- phone and email (from translation), each as clickable links (tel: and mailto:)
         - Column 3: Quick links -- repeat the main navigation links
       - Bottom bar: copyright text from translation + current year
       - Styling: bg-surface-800, border-t border-surface-700, py-12 px-6, text-text-secondary. Links: hover:text-accent-400 transition.

    4. Create `src/layouts/BaseLayout.astro` following the research pattern:
       - Props: title (string), description (string), pageKey (optional string)
       - In frontmatter: derive locale from URL using getLocaleFromUrl(), compute canonicalUrl
       - Import and render: global.css, @fontsource-variable/inter, @fontsource-variable/space-grotesk
       - `<html lang={locale}>` (use the locale directly -- "cs" or "en")
       - `<head>`: charset, viewport, title, meta description, canonical link
       - hreflang tags: Loop over locales array. For each locale, compute alternate path using getAlternatePath(). Generate `<link rel="alternate" hreflang={loc} href={absoluteUrl}>`. Also generate x-default pointing to Czech version.
       - `<slot name="head" />` for page-specific head content
       - `<body class="bg-surface-900 text-text-primary font-body min-h-screen flex flex-col">`
       - Render Header with locale and pageKey props
       - `<main class="flex-1">` with `<slot />` for page content
       - Render Footer with locale prop

    5. Update `src/pages/index.astro` (root redirect, should already exist from Plan 01):
       - Keep the redirect: `return Astro.redirect("/cs/", 302);`

    6. Create `src/pages/cs/index.astro` (Czech homepage placeholder):
       - Import BaseLayout
       - Wrap content in BaseLayout with title from t(locale, "site.title"), description from t(locale, "site.description"), pageKey="home"
       - Hero section: large heading with t(locale, "home.hero.title"), subtitle with t(locale, "home.hero.subtitle"), description paragraph with t(locale, "home.hero.description")
       - Hero styling: centered text, py-24 px-6, heading in text-5xl md:text-7xl font-heading font-bold, subtitle in text-xl md:text-2xl text-accent-400, description in text-lg text-text-secondary max-w-2xl mx-auto
       - Below hero: a simple "coming soon" section indicating more content is being built (use t() -- add a "home.coming_soon" key to both JSON files: "Obsah připravujeme" / "Content coming soon")
       - This is a PLACEHOLDER -- Phase 2 will replace it with real content. But it must demonstrate the full layout works.

    7. Create `src/pages/en/index.astro` (English homepage placeholder):
       - Same structure as cs/index.astro but no code duplication concern -- each page file is minimal (just imports layout and passes locale-derived props). The actual content rendering uses t() so the same code works for both.
       - In practice, the code is nearly identical to cs/index.astro. The only difference is that getLocaleFromUrl will return "en" for this page.

    IMPORTANT: Test with real Czech text (long navigation labels like "Certifikáty") to ensure the header does not overflow on mobile. Czech text is 15-30% longer than English -- this is a known pitfall from research.
  </action>
  <verify>
    Run `pnpm build` -- should succeed with all pages generated.
    Run `pnpm dev` and test:
    1. Visit http://localhost:4321/ -- should redirect to /cs/
    2. Visit http://localhost:4321/cs/ -- should show Czech homepage with dark industrial design, header with Czech nav labels, footer with contact info
    3. Visit http://localhost:4321/en/ -- should show English homepage with English labels
    4. Click language switcher on /cs/ -- should navigate to /en/
    5. Click language switcher on /en/ -- should navigate to /cs/
    6. View page source on /cs/ -- should contain hreflang tags for cs, en, and x-default
    7. Resize browser to 375px width -- header should show mobile menu, no horizontal scroll, layout stacks vertically
    8. Resize to 1440px -- three-column footer, horizontal navigation
  </verify>
  <done>
    BaseLayout renders with correct html lang, hreflang tags (cs, en, x-default), canonical URL, and design system styling. Header shows navigation with translated labels and working language switcher. Footer shows company contact info in translated text. Czech homepage at /cs/ and English homepage at /en/ both render correctly. Mobile responsive at 375px (no overflow, hamburger menu). Root / redirects to /cs/.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` produces static HTML for /cs/index.html and /en/index.html in dist/
- Both pages contain `<html lang="cs">` and `<html lang="en">` respectively
- Both pages contain three hreflang `<link>` tags (cs, en, x-default)
- Header navigation links use correct localized paths (/cs/produkt/ vs /en/product/)
- Language switcher correctly toggles between /cs/ and /en/ equivalents
- Footer displays company contact info (phone, email, address) in both languages
- No horizontal scroll at 375px, 768px, or 1440px viewport widths
- All visible text comes from translation JSON files (no hardcoded strings in .astro files)
- Fonts load: Space Grotesk for headings, Inter for body text
</verification>

<success_criteria>
1. i18n system operational: t() resolves keys, getLocaleFromUrl() extracts locale, getAlternatePath() maps between languages
2. Shared layout applied to both /cs/ and /en/ pages with consistent industrial design
3. Header with navigation + language switcher works on mobile and desktop
4. Footer with three-column layout and real contact data
5. hreflang tags present and correct on every page
6. Responsive at 375px, 768px, and 1440px -- no overflow, proper stacking
7. All text via t() -- zero hardcoded strings
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
