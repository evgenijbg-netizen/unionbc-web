---
phase: 01-foundation-design-system
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/pages/cs/index.astro
  - src/pages/en/index.astro
  - src/i18n/cs.json
  - src/i18n/en.json
autonomous: false

must_haves:
  truths:
    - "Site builds to static HTML with zero errors and zero TypeScript warnings"
    - "Visitor sees a polished, responsive bilingual site shell that looks like a serious industrial manufacturer"
    - "Site is deployed and accessible via HTTPS on Cloudflare Pages"
    - "All Phase 1 success criteria from ROADMAP.md are met"
  artifacts:
    - path: "dist/cs/index.html"
      provides: "Built Czech homepage with all layout, design, hreflang"
      contains: "hreflang"
    - path: "dist/en/index.html"
      provides: "Built English homepage with all layout, design, hreflang"
      contains: "hreflang"
    - path: "dist/_redirects"
      provides: "Old site redirect rules copied from public/"
      min_lines: 30
  key_links:
    - from: "dist/cs/index.html"
      to: "dist/en/index.html"
      via: "hreflang alternate link and language switcher href"
      pattern: "hreflang.*en"
    - from: "dist/_redirects"
      to: "old site URLs"
      via: "301 redirect rules"
      pattern: "301"
---

<objective>
Polish the homepage content, run a full build verification pass, deploy to Cloudflare Pages, and verify the site meets all Phase 1 success criteria. Human verifies the deployed site visually.

Purpose: Phase 1 is not complete until the site is deployed, served over HTTPS, and visually confirmed to look like a serious industrial manufacturer. This plan closes the loop.
Output: Deployed, verified bilingual site shell on Cloudflare Pages.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
@.planning/phases/01-foundation-design-system/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and homepage polish</name>
  <files>
    src/pages/cs/index.astro
    src/pages/en/index.astro
    src/i18n/cs.json
    src/i18n/en.json
  </files>
  <action>
    1. Run full build and type checks:
       - `pnpm astro check` -- fix any TypeScript errors
       - `pnpm build` -- verify clean build with zero errors
       - `pnpm biome check .` -- fix any lint/format issues with `pnpm biome check --write .`

    2. Verify built output structure:
       - dist/cs/index.html exists and contains: html lang="cs", hreflang tags (cs, en, x-default), canonical URL, meta description
       - dist/en/index.html exists and contains: html lang="en", hreflang tags, canonical URL, meta description
       - dist/_redirects exists with 30+ redirect rules
       - dist/robots.txt exists with sitemap reference
       - dist/index.html exists (root redirect page)

    3. Polish the homepage hero sections in both cs/index.astro and en/index.astro:
       - Ensure the hero has enough visual weight: large heading, accent-colored subtitle, descriptive paragraph
       - Add a "Request consultation" CTA button below the hero description: styled as inline-block bg-accent-500 hover:bg-accent-400 text-surface-900 font-heading font-semibold px-8 py-3 rounded-sm transition. Links to the contact page (getLocalizedPath("contact", locale)).
       - Add a section below the hero with 3 key stats in a responsive grid (3 cols on lg, 1 on mobile):
         - Stat 1: Temperature range "-60 °C to +260 °C" / "-60 °C to +260 °C"
         - Stat 2: Thermal conductivity "lambda = 0.001 W/m.C" / "lambda = 0.001 W/m.C"
         - Stat 3: Application "Nanasi se jako nater" / "Applied like paint"
       - Each stat card: bg-surface-800 border border-surface-700 rounded p-6 text-center. Number/value in text-2xl font-heading text-accent-400, label in text-sm text-text-secondary.
       - Add these stat strings to both cs.json and en.json translation files:
         - home.stats.temp_range: "-60 °C az +260 °C" / "-60 °C to +260 °C"
         - home.stats.temp_range_label: "Teplotni rozsah" / "Temperature range" (use proper diacritics in Czech)
         - home.stats.conductivity: "0.001 W/m·°C"
         - home.stats.conductivity_label: "Tepelna vodivost" / "Thermal conductivity" (use proper diacritics in Czech)
         - home.stats.application: "Nanasi se jako nater" / "Applied like paint" (use proper diacritics in Czech)
         - home.stats.application_label: "Aplikace" / "Application"

    4. Run `pnpm preview` to serve the built site locally. Verify:
       - /cs/ renders correctly
       - /en/ renders correctly
       - Language switcher works
       - Mobile responsive (no overflow at 375px)
       - Fonts load (Space Grotesk headings, Inter body)

    5. Deploy to Cloudflare Pages using wrangler:
       ```
       npx wrangler pages deploy dist --project-name unionbc-website
       ```
       If this is the first deployment, wrangler will prompt to create the project. If authentication is needed, Claude will create a dynamic auth checkpoint for the user to authenticate via `npx wrangler login`.
  </action>
  <verify>
    `pnpm build` completes with zero errors.
    `pnpm astro check` passes with zero errors.
    dist/ directory contains cs/index.html, en/index.html, _redirects, robots.txt.
    Both HTML files contain proper hreflang tags.
    Homepage hero section has CTA button and 3 stat cards.
    If deployment succeeded, the URL is noted for the checkpoint.
  </verify>
  <done>
    Clean build with zero errors. Homepage polished with CTA and key stats. Site deployed to Cloudflare Pages (or deployment command prepared if auth is needed).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of deployed site</name>
  <files>None -- verification only</files>
  <action>
    Present the deployed site to the user for visual and functional verification.

    What was built:
    - Complete Phase 1 bilingual site shell deployed to Cloudflare Pages
    - Industrial dark design with Space Grotesk headings and Inter body text
    - Bilingual Czech/English with language switcher
    - Responsive layout (mobile/tablet/desktop)
    - Header with navigation, footer with contact info
    - Homepage with hero, CTA button, and 3 key product stats
    - 31 old-site URL redirects configured
    - HTTPS via Cloudflare

    How to verify:
    1. Open the deployed Cloudflare Pages URL (shown in the deployment output)
    2. Verify Czech homepage (/cs/): dark industrial design, header with Czech nav labels and "English" switcher, hero with "TEMP-COAT TC101" heading and CTA button, three stat cards, footer with contact info
    3. Click "English" -- should navigate to /en/ with all English labels
    4. On mobile (DevTools, iPhone SE 375px): no horizontal scroll, hamburger menu, stat cards and footer stack vertically
    5. View page source -- confirm hreflang tags for cs, en, and x-default
    6. Try an old URL like /faq.html -- should 301 redirect to /cs/faq/
    7. Confirm HTTPS padlock in browser
    8. Check fonts: geometric headings (Space Grotesk), clean sans-serif body (Inter)
  </action>
  <verify>User confirms by typing "approved" or describes issues to fix.</verify>
  <done>User approves the deployed site. All Phase 1 success criteria verified by human inspection.</done>
</task>

</tasks>

<verification>
Phase 1 success criteria from ROADMAP.md:
1. Site renders correctly on mobile (375px), tablet (768px), and desktop (1440px) with no horizontal scroll or broken layouts
2. Visitor can switch between Czech and English using a header language switcher, with URL routing (/cs/, /en/) and proper hreflang tags on every page
3. All pages share a consistent industrial design system -- dark backgrounds, technical typography, accent colors -- that signals "serious manufacturer"
4. Any page loads in under 2 seconds on a throttled connection (static assets optimized, minimal JS shipped)
5. Site is served over HTTPS with a valid SSL certificate on Cloudflare Pages
</verification>

<success_criteria>
1. `pnpm build` and `pnpm astro check` both pass with zero errors
2. Homepage has polished hero with CTA and 3 key stats
3. Site deployed to Cloudflare Pages with HTTPS
4. Human confirms visual design, responsiveness, language switching, and redirects work
5. All 5 Phase 1 success criteria from ROADMAP.md verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-03-SUMMARY.md`
</output>
