---
phase: 02.1-visual-refresh-content-accuracy
plan: 05
type: execute
wave: 3
depends_on: ["02.1-01"]
files_modified:
  - package.json
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/assets/images/logo/union-bc-logo-D-dark.svg
  - src/assets/images/logo/union-bc-logo-D.svg
autonomous: true

must_haves:
  truths:
    - "Site uses Outfit font family for both headings and body text"
    - "Header displays Union B+C SVG logo (Variant D dark) instead of 'TEMP-COAT' text"
    - "Accent color is teal (#00B8A9 / oklch equivalent) matching brand manual, not generic blue"
    - "Dark surfaces (header/footer) use brand navy (#1B2A4A / oklch equivalent)"
    - "Secondary text uses brand Cool Grey (#7A8599 / oklch equivalent)"
    - "Site builds with 0 errors and all pages render correctly"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Brand-aligned OKLCH color palette and Outfit font"
      contains: "Outfit"
    - path: "src/components/Header.astro"
      provides: "SVG logo in header nav"
      contains: "union-bc-logo"
    - path: "src/assets/images/logo/union-bc-logo-D-dark.svg"
      provides: "Variant D logo for dark backgrounds (white text, teal +)"
  key_links:
    - from: "logo/union-bc-logo-D-dark.svg"
      to: "src/assets/images/logo/union-bc-logo-D-dark.svg"
      via: "copied from brand manual source"
    - from: "src/styles/global.css"
      to: "all components"
      via: "@theme design tokens"
      pattern: "--color-accent-, --font-"
---

<objective>
Align website with Union B+C brand manual: Outfit typeface, navy/teal color palette, and Variant D logo in header. Brand source files are in /logo/ directory.

Brand manual reference (from logo/union-bc-logo-v3.html):
- Font: Outfit (weights 300–800)
- Navy Primary: #1B2A4A
- Teal Accent: #00B8A9
- Cool Grey: #7A8599
- Light Grey: #B0B8C8
- Logo for web: Variant D (single-line "UNION B+C s.r.o.") with teal "+"
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Swap fonts from Space Grotesk + Inter to Outfit</name>
  <files>package.json, src/layouts/BaseLayout.astro, src/styles/global.css</files>
  <action>
    1. Run: pnpm remove @fontsource-variable/inter @fontsource-variable/space-grotesk
    2. Run: pnpm add @fontsource-variable/outfit
    3. In BaseLayout.astro: Replace the two font imports with single `import "@fontsource-variable/outfit";`
    4. In global.css @theme: Update both font tokens:
       - --font-heading: "Outfit Variable", ui-sans-serif, system-ui, sans-serif;
       - --font-body: "Outfit Variable", ui-sans-serif, system-ui, sans-serif;
    5. Verify build passes
  </action>
</task>

<task type="auto">
  <name>Task 2: Update color palette to brand manual colors</name>
  <files>src/styles/global.css</files>
  <action>
    Map brand hex colors to OKLCH and update @theme tokens:

    Dark surfaces (keep semantic scale, update hue/chroma to match navy #1B2A4A):
    - surface-900: Convert #1B2A4A to OKLCH (darkest - footer bg)
    - surface-800: Slightly lighter navy (header bg)
    - surface-700: Navy-tinted border color

    Accent (change from blue hue 250 to teal #00B8A9):
    - accent-500: Convert #00B8A9 to OKLCH (primary buttons, links)
    - accent-600: Darker teal for pressed state
    - accent-400: Lighter teal for hover state

    Text secondary:
    - text-secondary: Convert #7A8599 (Cool Grey) to OKLCH

    Keep unchanged:
    - surface-50, surface-100, surface-200 (light palette - these are neutral, brand-compatible)
    - text-primary, text-dark (dark text on light - already correct)
    - text-light (light text on dark - already correct)
    - warn-500 (CTA amber - separate from brand accent)

    Verify: Build passes, no color contrast issues
  </action>
</task>

<task type="auto">
  <name>Task 3: Add SVG logo to header and footer</name>
  <files>src/components/Header.astro, src/components/Footer.astro, src/assets/images/logo/</files>
  <action>
    1. Copy logo SVGs to asset directory:
       - logo/union-bc-logo-D-dark.svg → src/assets/images/logo/union-bc-logo-D-dark.svg
       - logo/union-bc-logo-D.svg → src/assets/images/logo/union-bc-logo-D.svg
    2. IMPORTANT: Remove the Google Fonts @import from SVG files (not needed, font loaded via fontsource).
       Replace font-family references with "Outfit Variable", sans-serif to match CSS.
    3. In Header.astro: Replace the "TEMP-COAT" text link with an inline SVG or <img> of Variant D dark logo.
       Size: approximately h-8 (32px height). Keep the link wrapper to homepage.
    4. In Footer.astro: Add logo Variant D (light version) above company info column.
    5. Verify: Logo renders on all pages, both languages, mobile and desktop
  </action>
</task>

</tasks>

<verification>
  build: "pnpm build must complete with 0 errors"
  visual:
    - "Header shows Union B+C logo (not 'TEMP-COAT')"
    - "Teal accent visible on buttons and links"
    - "Outfit font renders on headings and body text"
    - "Navy dark surfaces in header/footer"
    - "No broken layouts on mobile/desktop"
  automated:
    - "grep for 'TEMP-COAT' in Header.astro returns 0 matches"
    - "grep for 'Outfit' in global.css returns matches"
    - "grep for 'union-bc-logo' in Header.astro returns matches"
</verification>
